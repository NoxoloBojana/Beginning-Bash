# Question

What command would you use to remove the directory git-basic-exercises? Assume that this directory is NOT empty.

# Answer

Use rm -r git-basic-exercises to delete the entire folder and its contents. The -r flag means “recursive,” so it removes everything inside the folder too.